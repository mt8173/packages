package com.softwin.gbox.home.status;

public interface Tip {
	Tip setConfirmTip(boolean exist);
	Tip setBackTip(boolean exist);
	Tip setUninstalledTip(boolean exist);
	Tip setMenuTip(boolean exist);
}
