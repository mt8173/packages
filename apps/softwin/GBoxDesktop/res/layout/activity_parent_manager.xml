<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layout_activity_password"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <RelativeLayout
        android:id="@+id/parent_password_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_home_atet"
        android:visibility="visible" >

        <FrameLayout
            android:id="@+id/parent_password_frame"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_centerInParent="true">
        </FrameLayout>
    </RelativeLayout>

    <RelativeLayout
        android:id="@+id/parent_manager_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_home_atet"
        android:visibility="gone" >

        <FrameLayout
            android:id="@+id/parent_manager_frame"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_centerInParent="true"
            android:paddingLeft="50dip"
            android:paddingRight="50dip"
            android:paddingTop="@dimen/lock_layout_top" >
        </FrameLayout>
    </RelativeLayout>

</FrameLayout>